export interface Post {
    _id: string | number,
    title: string,
    image: string,
    rating: number,
}

export interface TSvgTypes {
    width?: string | number;
    height?: string | number;
    stroke?: string ;
}